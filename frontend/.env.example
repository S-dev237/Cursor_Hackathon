# URL du backend DDD (module IAM pour l'authentification réelle)
VITE_API_URL=http://localhost:8000/api/v1

# Mode démo pour les pages NON-auth (documents, recherche, admin).
# L'authentification (login/register/me/profil) utilise TOUJOURS le backend réel.
VITE_MOCK_API=true
